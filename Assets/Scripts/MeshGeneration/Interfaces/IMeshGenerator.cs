using UnityEngine;

namespace Voxels.MeshGeneration.Interfaces
{
    interface IMeshGenerator
    {
        // empty for now - Unity does not yet support internal interfaces
        //internal void CalculateMeshes(Vector3Int chunkPos, out Mesh terrain, out Mesh water);
    }
}
